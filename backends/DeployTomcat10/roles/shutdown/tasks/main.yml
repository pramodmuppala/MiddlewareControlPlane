---
- name: shutdown tomcat instance
  become: true
  become_user: parallels
  shell: "{{ tomcat.catalina_home }}/bin/shutdown.sh"
  environment:
    CATALINA_HOME: "{{ tomcat.catalina_home }}"
    CATALINA_BASE: "{{ tomcat.catalina_base }}"
  register: shutdown_result
  ignore_errors: true

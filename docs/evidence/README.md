# Evidence Output Directory

This directory is intentionally kept in the repository as the default destination for generated benchmark reports and decision-log evidence exports.

Typical generated files include:

- benchmark JSON summaries
- benchmark Markdown reports
- derived evidence artifacts for demos or case packets

The directory is git-ignored at the file level so generated outputs do not pollute the repository history.
